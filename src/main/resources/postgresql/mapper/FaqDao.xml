<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.amore.gps.service.faq.FaqDao">
	<select id="getFaqList" parameterType="CmMap" resultType="CmMap" flushCache="false">
	</select>
	<select id="getFaqCount" parameterType="CmMap" resultType="java.lang.Integer" flushCache="false">
	</select>	
</mapper>
